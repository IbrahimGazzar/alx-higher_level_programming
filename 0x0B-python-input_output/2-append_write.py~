#!/usr/bin/python3
"""
    This module handles file writing
"""


def write_file(filename="", text=""):
    """
        Write into a file

        Args:
            filename (str): name of the file
            text (str): text to be written
    """
    with open(filename, 'w', encoding="utf-8") as f:
        return f.write(text)
